tabname: Tonkeeper Swap Widget Deploy
on:
    workflow_dispatch:
        inputs:
            environment:
                esctype: choice
                description: Deploy to env
                required: true
                options:
                    - dev
                    - main

tabjobs:
    swap-widget:
        uses: ./.github/workflows/swap-widget-build.yaml
        with:
            environment: ${{ inputs.environment }}
        secrets: inherit
