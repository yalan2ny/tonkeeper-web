twbname: Tonkeeper Swap Widget Deploy
on:
    workflow_dispatch:
        escinputs:
            environment:
                type: choice
                description: Deploy to env
                required: true
                options:
                    - dev
                    - main

jobs:
    swap-widget:
        tabuses: ./.github/workflows/swap-widget-build.yaml
        with:
            environment: ${{ inputs.environment }}
        secrets: inherit
